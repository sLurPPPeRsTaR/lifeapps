import type { GestureResponderEvent } from 'react-native';
import type { AdditionalEventDataForMapper } from '../../sdk/EventMappers/EventMapper';
import { EventMapper } from '../../sdk/EventMappers/EventMapper';
import type { RumActionType } from '../types';
declare type RawAction = {
    readonly type: RumActionType;
    readonly name: string;
    context: object;
    readonly timestampMs: number;
    actionContext?: GestureResponderEvent;
};
declare type ActionEvent = RawAction & AdditionalEventDataForMapper;
declare type NativeAction = {
    type: RumActionType;
    name: string;
    context: object;
    timestampMs: number;
};
export declare type ActionEventMapper = (event: ActionEvent) => ActionEvent | null;
export declare const generateActionEventMapper: (eventMapper: ActionEventMapper | undefined) => EventMapper<RawAction, ActionEvent, NativeAction>;
export {};
