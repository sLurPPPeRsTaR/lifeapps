import type { RUMResource } from '../../domain/interfaces/RumResource';
declare type ResourceMapper = (resource: RUMResource) => RUMResource | null;
export declare class ResourceReporter {
    private mappers;
    constructor(resourceMappers: ResourceMapper[]);
    reportResource: (resource: RUMResource) => void;
}
export {};
