import { DatadogBuffer } from './DatadogBuffer';
export declare class BoundedBuffer extends DatadogBuffer {
    private bufferSize;
    private buffer;
    private idTable;
    private telemetryBuffer;
    constructor(bufferSize?: number);
    addCallback: (callback: () => Promise<void>) => Promise<void>;
    addCallbackReturningId: (callback: () => Promise<string>) => Promise<string>;
    addCallbackWithId: (callback: (id: string) => Promise<void>, bufferId: string) => Promise<void>;
    drain: () => Promise<void>;
    private generateRandomBufferId;
    private addTelemetryEvent;
    private drainTelemetry;
}
