import Color from 'ca-config/Color';
import Size from 'ca-config/Size';

export default {
  container: {
    minHeight: Size.screen.height - 100,
    backgroundColor: Color.whiteCard.light.color,
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
  },
  mt24: {
    marginTop: 24,
  },
};
